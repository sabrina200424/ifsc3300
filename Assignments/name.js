const wholename = "Sabrina Kabir";
const nameUpper = name.toUpperCase();
const nameLength = name.length;        // nameLength = 13
const index = name.indexOf(" ");       // index = 5
const firstName = name.substr(0, index);
firstName = wholename.substring(0,7)